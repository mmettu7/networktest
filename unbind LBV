- hosts: localhost
  tasks:
    - name: Check LBVserver binding to CSVserver
      delegate_to: localhost
      community.network.netscaler_csvserver_lbvserver_binding:
        nsip: "{{ netscaler_ip }}"
        nitro_user: "{{ nitro_user }}"
        nitro_pass: "{{ nitro_pass }}"
        name: "{{ csvserver_name }}"
        lbvserver: "{{ lbvserver_name }}"
